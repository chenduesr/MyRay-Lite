<Window x:Class="V2RayLite.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:V2RayLite.App"
        xmlns:core="clr-namespace:V2RayLite.Core;assembly=V2RayLite.Core"
        Title="MyRay Lite"
        Width="1280"
        Height="820"
        MinWidth="1100"
        MinHeight="720"
        WindowStyle="None"
        ResizeMode="CanResizeWithGrip"
        Background="Transparent"
        AllowsTransparency="True"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        FontFamily="Microsoft YaHei UI">
    <Window.Resources>
        <SolidColorBrush x:Key="BlueBrush" Color="#0875F8" />
        <SolidColorBrush x:Key="BlueHoverBrush" Color="#0068E6" />
        <SolidColorBrush x:Key="TextBrush" Color="#111827" />
        <SolidColorBrush x:Key="SubtleTextBrush" Color="#64748B" />
        <SolidColorBrush x:Key="SidebarTextBrush" Color="#334155" />
        <SolidColorBrush x:Key="SurfaceBrush" Color="#F6F8FC" />
        <SolidColorBrush x:Key="PanelBrush" Color="#FFFFFF" />
        <SolidColorBrush x:Key="LineBrush" Color="#E1E8F2" />

        <Style TargetType="Control">
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        </Style>

        <Style x:Key="IconText" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets" />
            <Setter Property="FontSize" Value="22" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="TextAlignment" Value="Center" />
        </Style>

        <Style x:Key="WindowButton" TargetType="Button">
            <Setter Property="Width" Value="44" />
            <Setter Property="Height" Value="36" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="Foreground" Value="#0875F8" />
            <Setter Property="FontSize" Value="18" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root" Background="{TemplateBinding Background}" CornerRadius="7">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#EAF3FF" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#DCEBFF" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="NavButton" TargetType="Button">
            <Setter Property="Height" Value="58" />
            <Setter Property="Margin" Value="24,4" />
            <Setter Property="Padding" Value="20,0" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="Foreground" Value="{StaticResource SidebarTextBrush}" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root" Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#EEF5FF" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Height" Value="54" />
            <Setter Property="MinWidth" Value="132" />
            <Setter Property="Padding" Value="22,0" />
            <Setter Property="Background" Value="{StaticResource BlueBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource BlueBrush}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontSize" Value="18" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{StaticResource BlueHoverBrush}" />
                                <Setter TargetName="Root" Property="BorderBrush" Value="{StaticResource BlueHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Opacity" Value="0.88" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Root" Property="Background" Value="#E8EEF6" />
                                <Setter TargetName="Root" Property="BorderBrush" Value="#E8EEF6" />
                                <Setter Property="Foreground" Value="#9AA6B2" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="OutlineButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="White" />
            <Setter Property="Foreground" Value="{StaticResource BlueBrush}" />
            <Setter Property="BorderBrush" Value="#D5DFEC" />
            <Setter Property="FontSize" Value="17" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>

        <Style x:Key="GhostButton" TargetType="Button">
            <Setter Property="Height" Value="48" />
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="#D5DFEC" />
            <Setter Property="Foreground" Value="{StaticResource BlueBrush}" />
            <Setter Property="FontSize" Value="17" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#F6FAFF" />
                                <Setter TargetName="Root" Property="BorderBrush" Value="#B9D7FF" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource PanelBrush}" />
            <Setter Property="BorderBrush" Value="#DDE6F1" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="10" />
            <Setter Property="Padding" Value="28" />
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="18" ShadowDepth="6" Opacity="0.06" Color="#64748B" />
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Height" Value="50" />
            <Setter Property="Padding" Value="16,0" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="BorderBrush" Value="#D5DFEC" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Background" Value="White" />
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="FontSize" Value="17" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="Root" Property="BorderBrush" Value="#92C2FF" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="FontSize" Value="18" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid Height="34">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="54" />
                            </Grid.ColumnDefinitions>
                            <ContentPresenter VerticalAlignment="Center" />
                            <Border x:Name="Track" Grid.Column="1" Width="46" Height="24" CornerRadius="12" Background="#DCE6F2" HorizontalAlignment="Right">
                                <Ellipse x:Name="Thumb" Width="20" Height="20" Fill="White" Margin="2" HorizontalAlignment="Left">
                                    <Ellipse.Effect>
                                        <DropShadowEffect BlurRadius="6" ShadowDepth="1" Opacity="0.18" />
                                    </Ellipse.Effect>
                                </Ellipse>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Track" Property="Background" Value="{StaticResource BlueBrush}" />
                                <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="RadioButton">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="FontSize" Value="18" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <StackPanel Orientation="Horizontal" Height="34" VerticalAlignment="Center">
                            <Border Width="20" Height="20" CornerRadius="10" BorderBrush="#A8B4C3" BorderThickness="2" Background="White" VerticalAlignment="Center">
                                <Ellipse x:Name="Dot" Fill="{StaticResource BlueBrush}" Width="10" Height="10" Visibility="Collapsed" />
                            </Border>
                            <ContentPresenter Margin="12,0,0,0" VerticalAlignment="Center" />
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Dot" Property="Visibility" Value="Visible" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Margin="36"
            Background="{StaticResource SurfaceBrush}"
            BorderBrush="#C9D4E2"
            BorderThickness="1"
            CornerRadius="14"
            MouseLeftButtonDown="WindowDragArea_MouseLeftButtonDown">
        <Border.Effect>
            <DropShadowEffect BlurRadius="34" ShadowDepth="14" Opacity="0.13" Color="#475569" />
        </Border.Effect>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="260" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#F8FAFD" BorderBrush="#DCE5F0" BorderThickness="0,0,1,0" CornerRadius="14,0,0,14">
                <Grid Margin="0,32,0,30">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="92" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="128" />
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal" Margin="28,0,0,0" VerticalAlignment="Top">
                        <Border Width="42" Height="42" CornerRadius="10" Background="{StaticResource BlueBrush}">
                            <TextBlock Text="M" FontSize="27" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <TextBlock Text="MyRay Lite" Margin="14,6,0,0" FontSize="24" FontWeight="Bold" Foreground="{StaticResource BlueBrush}" VerticalAlignment="Top" />
                    </StackPanel>

                    <StackPanel Grid.Row="1" Margin="0,18,0,0">
                        <Button Style="{StaticResource NavButton}" Tag="Home" Click="Navigate_Click" Background="{Binding HomeNavBackground}" Foreground="{Binding HomeNavForeground}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Style="{StaticResource IconText}" Text="&#xE80F;" Width="38" />
                                <TextBlock Text="首页" FontSize="19" Margin="16,0,0,0" VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>
                        <Button Style="{StaticResource NavButton}" Tag="Nodes" Click="Navigate_Click" Background="{Binding NodesNavBackground}" Foreground="{Binding NodesNavForeground}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Style="{StaticResource IconText}" Text="&#xE8B7;" Width="38" />
                                <TextBlock Text="节点" FontSize="19" Margin="16,0,0,0" VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>
                        <Button Style="{StaticResource NavButton}" Tag="Subscription" Click="Navigate_Click" Background="{Binding SubscriptionNavBackground}" Foreground="{Binding SubscriptionNavForeground}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Style="{StaticResource IconText}" Text="&#xEBD3;" Width="38" />
                                <TextBlock Text="订阅" FontSize="19" Margin="16,0,0,0" VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>
                        <Button Style="{StaticResource NavButton}" Tag="Logs" Click="Navigate_Click" Background="{Binding LogsNavBackground}" Foreground="{Binding LogsNavForeground}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Style="{StaticResource IconText}" Text="&#xE9D9;" Width="38" />
                                <TextBlock Text="日志" FontSize="19" Margin="16,0,0,0" VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>
                        <Button Style="{StaticResource NavButton}" Tag="Settings" Click="Navigate_Click" Background="{Binding SettingsNavBackground}" Foreground="{Binding SettingsNavForeground}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Style="{StaticResource IconText}" Text="&#xE713;" Width="38" />
                                <TextBlock Text="设置" FontSize="19" Margin="16,0,0,0" VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>
                    </StackPanel>

                    <StackPanel Grid.Row="2" Margin="28,0">
                        <Border Height="1" Background="#DDE6F1" Margin="0,0,0,20" />
                        <Grid Margin="0,0,0,20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal">
                                <Ellipse Fill="{Binding SidebarStatusBrush}" Width="10" Height="10" Margin="0,0,10,0" VerticalAlignment="Center" />
                                <TextBlock Text="{Binding SidebarStatus}" FontSize="15" Foreground="#526174" VerticalAlignment="Center" />
                            </StackPanel>
                            <TextBlock Grid.Column="1" Text="v1.0.0" FontSize="15" Foreground="#526174" />
                        </Grid>
                        <Grid Height="34">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Style="{StaticResource IconText}" Text="&#xE7C3;" Foreground="#526174" Width="28" FontSize="20" />
                                <TextBlock Text="开机自启" FontSize="16" Margin="8,0,0,0" Foreground="#526174" VerticalAlignment="Center" />
                            </StackPanel>
                            <CheckBox x:Name="SidebarStartupCheckBox" Width="54" HorizontalAlignment="Right" Click="StartupCheckBox_Click" IsChecked="True" />
                        </Grid>
                    </StackPanel>
                </Grid>
            </Border>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="72" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,22,28,0">
                    <Button Style="{StaticResource WindowButton}" Content="-" Click="Minimize_Click" />
                    <Button Style="{StaticResource WindowButton}" Content="□" Click="Maximize_Click" />
                    <Button Style="{StaticResource WindowButton}" Content="×" Click="Close_Click" FontSize="22" />
                </StackPanel>

                <Grid Grid.Row="1" Margin="44,0,44,36">
                    <Grid Visibility="{Binding CurrentPage, Converter={StaticResource PageVisibilityConverter}, ConverterParameter=Home}">
                        <Grid HorizontalAlignment="Center" Width="760">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <TextBlock Text="系统代理" FontSize="44" FontWeight="Black" Foreground="{StaticResource TextBrush}" TextAlignment="Center" Margin="0,46,0,22" />
                            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,28">
                                <TextBlock Text="当前状态：" FontSize="21" Foreground="{StaticResource TextBrush}" />
                                <TextBlock Text="{Binding RuntimeStatus}" FontSize="21" FontWeight="Bold" Foreground="{Binding RuntimeStatusBrush}" Margin="18,0,0,0" />
                            </StackPanel>
                            <Button Grid.Row="2" Style="{StaticResource PrimaryButton}" Width="384" Height="68" HorizontalAlignment="Center" Click="ToggleProxy_Click">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <TextBlock Style="{StaticResource IconText}" Text="&#xE7E8;" Foreground="White" FontSize="32" />
                                    <TextBlock Text="{Binding ToggleProxyText}" FontSize="26" FontWeight="Bold" Margin="20,0,0,0" VerticalAlignment="Center" />
                                </StackPanel>
                            </Button>

                            <StackPanel Grid.Row="3" Margin="0,40,0,0">
                                <Border Style="{StaticResource Card}" Width="640" HorizontalAlignment="Center" Padding="28,22">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="46" />
                                            <ColumnDefinition Width="150" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="56" />
                                            <RowDefinition Height="56" />
                                            <RowDefinition Height="56" />
                                            <RowDefinition Height="56" />
                                        </Grid.RowDefinitions>
                                        <TextBlock Grid.Row="0" Style="{StaticResource IconText}" Text="&#xE707;" Foreground="{StaticResource BlueBrush}" />
                                        <TextBlock Grid.Row="0" Grid.Column="1" Text="当前节点：" FontSize="20" FontWeight="SemiBold" VerticalAlignment="Center" />
                                        <TextBlock Grid.Row="0" Grid.Column="2" Text="{Binding ActiveNodeName}" FontSize="20" Foreground="{StaticResource BlueBrush}" FontWeight="SemiBold" VerticalAlignment="Center" HorizontalAlignment="Right" TextTrimming="CharacterEllipsis" />
                                        <Border Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2" Background="{StaticResource LineBrush}" Height="1" VerticalAlignment="Bottom" />

                                        <TextBlock Grid.Row="1" Style="{StaticResource IconText}" Text="&#xE916;" Foreground="#18AE4D" />
                                        <TextBlock Grid.Row="1" Grid.Column="1" Text="延迟：" FontSize="20" FontWeight="SemiBold" VerticalAlignment="Center" />
                                        <TextBlock Grid.Row="1" Grid.Column="2" Text="{Binding ActiveNodeDelay}" FontSize="20" Foreground="#18AE4D" FontWeight="SemiBold" VerticalAlignment="Center" HorizontalAlignment="Right" />
                                        <Border Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" Background="{StaticResource LineBrush}" Height="1" VerticalAlignment="Bottom" />

                                        <TextBlock Grid.Row="2" Style="{StaticResource IconText}" Text="&#xE9D9;" Foreground="#635BFF" />
                                        <TextBlock Grid.Row="2" Grid.Column="1" Text="模式：" FontSize="20" FontWeight="SemiBold" VerticalAlignment="Center" />
                                        <TextBlock Grid.Row="2" Grid.Column="2" Text="{Binding ProxyModeText}" FontSize="20" Foreground="{StaticResource BlueBrush}" FontWeight="SemiBold" VerticalAlignment="Center" HorizontalAlignment="Right" />
                                        <Border Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2" Background="{StaticResource LineBrush}" Height="1" VerticalAlignment="Bottom" />

                                        <TextBlock Grid.Row="3" Style="{StaticResource IconText}" Text="&#xE8B7;" Foreground="{StaticResource BlueBrush}" />
                                        <TextBlock Grid.Row="3" Grid.Column="1" Text="协议核心：" FontSize="20" FontWeight="SemiBold" VerticalAlignment="Center" />
                                        <TextBlock Grid.Row="3" Grid.Column="2" Text="Xray" FontSize="20" Foreground="{StaticResource BlueBrush}" FontWeight="SemiBold" VerticalAlignment="Center" HorizontalAlignment="Right" />
                                    </Grid>
                                </Border>

                                <UniformGrid Columns="3" Rows="1" Width="640" HorizontalAlignment="Center" Margin="0,26,0,0">
                                    <Button Style="{StaticResource OutlineButton}" Height="54" Margin="0,0,12,0" Click="SwitchNode_Click" Content="切换节点" />
                                    <Button Style="{StaticResource OutlineButton}" Height="54" Margin="6,0" Click="UpdateSubscription_Click" Content="更新订阅" />
                                    <Button Style="{StaticResource OutlineButton}" Height="54" Margin="12,0,0,0" Click="TestActiveNode_Click" Content="测试延迟" />
                                </UniformGrid>
                            </StackPanel>
                        </Grid>
                    </Grid>

                    <Grid Visibility="{Binding CurrentPage, Converter={StaticResource PageVisibilityConverter}, ConverterParameter=Nodes}">
                        <Grid Width="880" HorizontalAlignment="Center" Margin="0,28,0,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <TextBlock Text="节点列表" FontSize="42" FontWeight="Black" Foreground="{StaticResource TextBrush}" Margin="0,20,0,28" />
                            <Grid Grid.Row="1" Margin="0,0,0,22">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="76" />
                                    <ColumnDefinition Width="170" />
                                </Grid.ColumnDefinitions>
                                <Grid>
                                    <TextBox x:Name="SearchTextBox" TextChanged="SearchTextBox_TextChanged" Height="54" FontSize="17" Padding="48,0,16,0" />
                                    <TextBlock Style="{StaticResource IconText}" Text="&#xE721;" Foreground="#718096" HorizontalAlignment="Left" Width="48" />
                                    <TextBlock Text="搜索节点（名称、地址、备注）" IsHitTestVisible="False" Margin="48,0,0,0" VerticalAlignment="Center" Foreground="#9AA6B2" FontSize="17" Visibility="{Binding IsSearchEmpty, Converter={StaticResource BoolVisibilityConverter}}" />
                                </Grid>
                                <Button Grid.Column="1" Style="{StaticResource GhostButton}" Height="54" Margin="18,0,0,0" Click="RefreshNodes_Click">
                                    <TextBlock Style="{StaticResource IconText}" Text="&#xE72C;" Foreground="{StaticResource BlueBrush}" />
                                </Button>
                                <Button Grid.Column="2" Style="{StaticResource PrimaryButton}" Height="54" Margin="18,0,0,0" Click="TestAllNodes_Click" Content="测速" />
                            </Grid>

                            <Border Grid.Row="2" Style="{StaticResource Card}" Padding="12" MinHeight="390">
                                <Grid>
                                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                                        <ItemsControl ItemsSource="{Binding FilteredNodes}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate DataType="{x:Type core:ProxyNode}">
                                                    <Border CornerRadius="8" Background="White" MinHeight="68" Margin="0,0,0,2">
                                                        <Grid>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="34" />
                                                                <ColumnDefinition Width="48" />
                                                                <ColumnDefinition Width="*" />
                                                                <ColumnDefinition Width="118" />
                                                                <ColumnDefinition Width="124" />
                                                                <ColumnDefinition Width="34" />
                                                            </Grid.ColumnDefinitions>
                                                            <Ellipse Width="10" Height="10" Fill="{Binding Status, Converter={x:Static local:NodeStatusBrushConverter.Instance}}" VerticalAlignment="Center" HorizontalAlignment="Center" />
                                                            <Border Grid.Column="1" Width="36" Height="36" Background="#F0F5FB" CornerRadius="18" VerticalAlignment="Center" HorizontalAlignment="Center">
                                                                <TextBlock Text="{Binding Name, Converter={x:Static local:FlagTextConverter.Instance}}" FontSize="15" FontWeight="Bold" Foreground="{StaticResource BlueBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                                            </Border>
                                                            <Button Grid.Column="2" Background="Transparent" BorderBrush="Transparent" HorizontalContentAlignment="Left" Padding="4,0" Click="CopyNode_Click" Tag="{Binding Id}">
                                                                <TextBlock Text="{Binding Name}" FontSize="20" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" TextTrimming="CharacterEllipsis" />
                                                            </Button>
                                                            <TextBlock Grid.Column="3" Text="{Binding DisplayDelay}" FontSize="18" Foreground="{Binding Status, Converter={x:Static local:NodeDelayBrushConverter.Instance}}" FontWeight="SemiBold" VerticalAlignment="Center" HorizontalAlignment="Center" />
                                                            <Button Grid.Column="4" Height="40" Width="104" Click="ConnectNode_Click" Tag="{Binding Id}" Content="{Binding IsActive, Converter={x:Static local:NodeActionTextConverter.Instance}}" Style="{StaticResource OutlineButton}" FontSize="16" />
                                                            <TextBlock Grid.Column="5" Style="{StaticResource IconText}" Text="&#xE76C;" Foreground="#B7C2D0" FontSize="18" />
                                                            <Border Grid.Column="1" Grid.ColumnSpan="5" Height="1" Background="#EEF2F7" VerticalAlignment="Bottom" />
                                                        </Grid>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </ScrollViewer>
                                    <StackPanel Visibility="{Binding HasNodes, Converter={StaticResource InverseBoolVisibilityConverter}}" HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <TextBlock Style="{StaticResource IconText}" Text="&#xE8B7;" Foreground="#B8C4D2" FontSize="42" HorizontalAlignment="Center" Margin="0,0,0,12" />
                                        <TextBlock Text="暂无节点" Foreground="#526174" FontSize="22" FontWeight="SemiBold" HorizontalAlignment="Center" />
                                        <TextBlock Text="请在订阅页保存并更新订阅" Foreground="#8B97A8" FontSize="16" HorizontalAlignment="Center" Margin="0,8,0,0" />
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </Grid>
                    </Grid>

                    <Grid Visibility="{Binding CurrentPage, Converter={StaticResource PageVisibilityConverter}, ConverterParameter=Subscription}">
                        <StackPanel Width="640" HorizontalAlignment="Center" Margin="0,44,0,0">
                            <TextBlock Text="订阅管理" FontSize="44" FontWeight="Black" Foreground="{StaticResource TextBrush}" TextAlignment="Center" Margin="0,24,0,38" />
                            <Border Style="{StaticResource Card}" Margin="0,0,0,28">
                                <StackPanel>
                                    <TextBlock Text="订阅地址" FontSize="20" FontWeight="Bold" Foreground="{StaticResource TextBrush}" Margin="0,0,0,16" />
                                    <TextBox x:Name="SubscriptionUrlTextBox" Height="52" FontSize="18" />
                                    <Grid Margin="0,26,0,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <Button Style="{StaticResource PrimaryButton}" Height="54" Margin="0,0,10,0" Click="SaveSubscription_Click" Content="保存" />
                                        <Button Grid.Column="1" Style="{StaticResource OutlineButton}" Height="54" Margin="10,0,0,0" Click="UpdateSubscription_Click" Content="更新订阅" />
                                    </Grid>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource Card}" Padding="28,20">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="46" />
                                        <ColumnDefinition Width="150" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="56" />
                                        <RowDefinition Height="56" />
                                        <RowDefinition Height="56" />
                                        <RowDefinition Height="56" />
                                    </Grid.RowDefinitions>
                                    <TextBlock Grid.Row="0" Style="{StaticResource IconText}" Text="&#xE8B7;" Foreground="{StaticResource BlueBrush}" />
                                    <TextBlock Grid.Row="0" Grid.Column="1" Text="节点数量：" FontSize="19" FontWeight="SemiBold" VerticalAlignment="Center" />
                                    <TextBlock Grid.Row="0" Grid.Column="2" Text="{Binding NodeCount}" FontSize="19" Foreground="{StaticResource BlueBrush}" FontWeight="SemiBold" VerticalAlignment="Center" HorizontalAlignment="Right" />
                                    <Border Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2" Background="{StaticResource LineBrush}" Height="1" VerticalAlignment="Bottom" />

                                    <TextBlock Grid.Row="1" Style="{StaticResource IconText}" Text="&#xE916;" Foreground="{StaticResource BlueBrush}" />
                                    <TextBlock Grid.Row="1" Grid.Column="1" Text="上次更新：" FontSize="19" FontWeight="SemiBold" VerticalAlignment="Center" />
                                    <TextBlock Grid.Row="1" Grid.Column="2" Text="{Binding LastUpdateText}" FontSize="18" Foreground="{StaticResource BlueBrush}" FontWeight="SemiBold" VerticalAlignment="Center" HorizontalAlignment="Right" />
                                    <Border Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" Background="{StaticResource LineBrush}" Height="1" VerticalAlignment="Bottom" />

                                    <TextBlock Grid.Row="2" Style="{StaticResource IconText}" Text="&#xE9D9;" Foreground="{StaticResource BlueBrush}" />
                                    <TextBlock Grid.Row="2" Grid.Column="1" Text="协议核心：" FontSize="19" FontWeight="SemiBold" VerticalAlignment="Center" />
                                    <TextBlock Grid.Row="2" Grid.Column="2" Text="Xray" FontSize="19" Foreground="{StaticResource BlueBrush}" FontWeight="SemiBold" VerticalAlignment="Center" HorizontalAlignment="Right" />
                                    <Border Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2" Background="{StaticResource LineBrush}" Height="1" VerticalAlignment="Bottom" />

                                    <TextBlock Grid.Row="3" Style="{StaticResource IconText}" Text="&#xE73E;" Foreground="{StaticResource BlueBrush}" />
                                    <TextBlock Grid.Row="3" Grid.Column="1" Text="订阅状态：" FontSize="19" FontWeight="SemiBold" VerticalAlignment="Center" />
                                    <TextBlock Grid.Row="3" Grid.Column="2" Text="{Binding SubscriptionStatus}" FontSize="19" Foreground="{Binding SubscriptionStatusBrush}" FontWeight="SemiBold" VerticalAlignment="Center" HorizontalAlignment="Right" />
                                </Grid>
                            </Border>
                        </StackPanel>
                    </Grid>

                    <Grid Visibility="{Binding CurrentPage, Converter={StaticResource PageVisibilityConverter}, ConverterParameter=Settings}">
                        <StackPanel Width="820" HorizontalAlignment="Center" Margin="0,32,0,0">
                            <TextBlock Text="设置" FontSize="42" FontWeight="Black" Foreground="{StaticResource TextBrush}" Margin="0,18,0,28" />
                            <Border Style="{StaticResource Card}" Margin="0,0,0,22">
                                <StackPanel>
                                    <TextBlock Text="代理模式" FontSize="21" FontWeight="Bold" Margin="0,0,0,24" />
                                    <UniformGrid Columns="3">
                                        <RadioButton x:Name="RuleModeRadio" Content="规则模式" GroupName="ProxyMode" Checked="ProxyMode_Checked" Tag="Rule" />
                                        <RadioButton x:Name="GlobalModeRadio" Content="全局模式" GroupName="ProxyMode" Checked="ProxyMode_Checked" Tag="Global" />
                                        <RadioButton x:Name="DirectModeRadio" Content="直连模式" GroupName="ProxyMode" Checked="ProxyMode_Checked" Tag="Direct" />
                                    </UniformGrid>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource Card}" Margin="0,0,0,22">
                                <StackPanel>
                                    <TextBlock Text="系统设置" FontSize="21" FontWeight="Bold" Margin="0,0,0,18" />
                                    <CheckBox x:Name="StartOnBootCheckBox" Content="开机自启" Margin="0,8" Click="StartupCheckBox_Click" />
                                    <Border Height="1" Background="{StaticResource LineBrush}" />
                                    <CheckBox x:Name="AutoConnectCheckBox" Content="启动后自动连接" Margin="0,8" Click="SettingsCheckBox_Click" />
                                    <Border Height="1" Background="{StaticResource LineBrush}" />
                                    <CheckBox x:Name="MinimizeToTrayCheckBox" Content="最小化到托盘" Margin="0,8" Click="SettingsCheckBox_Click" />
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource Card}" Margin="0,0,0,22">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="110" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="58" />
                                        <RowDefinition Height="58" />
                                        <RowDefinition Height="58" />
                                    </Grid.RowDefinitions>
                                    <TextBlock Text="HTTP:" FontSize="18" FontWeight="SemiBold" VerticalAlignment="Center" />
                                    <TextBox x:Name="HttpPortTextBox" Grid.Column="1" TextChanged="PortTextBox_TextChanged" />
                                    <TextBlock Grid.Row="1" Text="SOCKS:" FontSize="18" FontWeight="SemiBold" VerticalAlignment="Center" />
                                    <TextBox x:Name="SocksPortTextBox" Grid.Row="1" Grid.Column="1" TextChanged="PortTextBox_TextChanged" />
                                    <TextBlock Grid.Row="2" Text="测速URL:" FontSize="18" FontWeight="SemiBold" VerticalAlignment="Center" />
                                    <TextBox x:Name="DelayTestUrlTextBox" Grid.Row="2" Grid.Column="1" TextChanged="DelayTestUrlTextBox_TextChanged" />
                                </Grid>
                            </Border>

                            <Border Style="{StaticResource Card}" Padding="28,18">
                                <Grid Height="42">
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="协议核心：" FontSize="18" FontWeight="SemiBold" />
                                        <TextBlock Text="Xray" FontSize="18" Foreground="{StaticResource BlueBrush}" FontWeight="SemiBold" Margin="18,0,0,0" />
                                    </StackPanel>
                                    <Button Content="下载核心" Style="{StaticResource OutlineButton}" Width="132" Height="42" HorizontalAlignment="Right" Click="DownloadXray_Click" />
                                </Grid>
                            </Border>
                        </StackPanel>
                    </Grid>

                    <Grid Visibility="{Binding CurrentPage, Converter={StaticResource PageVisibilityConverter}, ConverterParameter=Logs}">
                        <Grid Width="880" HorizontalAlignment="Center" Margin="0,32,0,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <TextBlock Text="日志诊断" FontSize="42" FontWeight="Black" Foreground="{StaticResource TextBrush}" Margin="0,18,0,24" />
                            <Grid Grid.Row="1" Margin="0,0,0,18">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="130" />
                                    <ColumnDefinition Width="130" />
                                    <ColumnDefinition Width="130" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="显示应用日志、Xray 启动日志和测速诊断。Xray 启动失败时优先看这里。" Foreground="{StaticResource SubtleTextBrush}" FontSize="16" VerticalAlignment="Center" />
                                <Button Grid.Column="1" Style="{StaticResource OutlineButton}" Height="44" Margin="12,0,0,0" Click="RefreshLogs_Click" Content="刷新" />
                                <Button Grid.Column="2" Style="{StaticResource OutlineButton}" Height="44" Margin="12,0,0,0" Click="ClearLogs_Click" Content="清空" />
                                <Button Grid.Column="3" Style="{StaticResource PrimaryButton}" Height="44" Margin="12,0,0,0" Click="OpenLogFolder_Click" Content="打开目录" />
                            </Grid>
                            <Border Grid.Row="2" Style="{StaticResource Card}" Padding="0" MinHeight="430">
                                <Grid>
                                    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="18">
                                        <ItemsControl ItemsSource="{Binding LogLines}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="{Binding}" FontFamily="Consolas" FontSize="13" Foreground="#263445" TextWrapping="Wrap" Margin="0,0,0,4" />
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </ScrollViewer>
                                    <TextBlock Text="暂无日志" Visibility="{Binding HasLogs, Converter={StaticResource InverseBoolVisibilityConverter}}" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#8B97A8" FontSize="18" />
                                </Grid>
                            </Border>
                        </Grid>
                    </Grid>
                </Grid>

                <TextBlock Text="{Binding ToastMessage}" Foreground="#526174" FontSize="15" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,16" />
            </Grid>
        </Grid>
    </Border>
</Window>
